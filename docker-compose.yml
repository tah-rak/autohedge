services:
  autohedge:
    build:
      context: .
      target: python-runtime
    ports:
      - "8000:8000"
    volumes:
      - ./outputs:/app/outputs
      - ./configs:/app/configs
    command: ["autohedge", "serve", "--host", "0.0.0.0", "--port", "8000"]
